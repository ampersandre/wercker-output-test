box: node

build:
  steps:
    - npm-install

    - script:
      name: build
      code: npm run build

other-build:
  steps:
    - npm-install

    - script:
      name: build
      code: npm run build
